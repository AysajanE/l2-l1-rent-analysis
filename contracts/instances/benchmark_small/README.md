# `contracts/instances/benchmark_small/` — golden benchmark (tracked)

Tiny benchmark instances for modeling/hybrid projects.

Rules:
- Keep this set small and deterministic.
- Treat it as part of the contract surface; changes require a manifest and changelog entry.
