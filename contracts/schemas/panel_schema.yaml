version: 1
description: "Canonical analysis panel schema (edit per project)."
fields:
  - name: date
    type: date
    description: "UTC date (YYYY-MM-DD)"
  - name: unit_id
    type: string
    description: "Stable unit identifier (e.g., rollup_id, address, region)"
