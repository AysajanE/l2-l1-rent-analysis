version: 1
description: "Schema entrypoints (use versioned schema files for canonical field lists)."
schemas:
  str: "contracts/schemas/panel_schema_str_v1.yaml"
  decomposition: "contracts/schemas/panel_schema_decomp_v1.yaml"
