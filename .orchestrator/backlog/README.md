# Backlog

Tasks waiting to be started.

- Create new tasks by copying `.orchestrator/templates/task_template.md`.
- Keep tasks small and interface-driven (clear inputs/outputs, strict file ownership).
